# Copilot Instructions

- Use Yarn for package management and scripts in this repository.
- Prefer `yarn <script>` over `npm run <script>`.
- Prefer `yarn add` / `yarn remove` over npm equivalents.
- Avoid introducing npm-specific commands unless explicitly requested.
- Use UK English spelling and grammar in comments and documentation.
